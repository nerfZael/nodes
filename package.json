{
  "name": "@polywrap/nodes",
  "version": "0.1.0",
  "main": "bin/main.js",
  "types": "bin/main.d.ts",
  "author": "nerfZael",
  "license": "MIT",
  "private": false,
  "files": [
    "/bin"
  ],
  "bin": {
    "wrap-persistence-node": "./bin/main.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/polywrap/nodes"
  },
  "keywords": [
    "polywrap",
    "ipfs",
    "persistence",
    "wrap",
    "ens",
    "polywrapper",
    "wrapper"
  ],
  "scripts": {
    "build": "yarn build:persistence && yarn build:ens",
    "build:persistence": "cd ./nodes/persistence-node && tsc --project tsconfig.build.json && yarn copy-ui-files",
    "build:ens": "cd ./nodes/ens-contenthash-indexer-node && tsc --project tsconfig.build.json && yarn copy-ui-files",
    "install:ci": "yarn persistence:install:ci && yarn ens:install:ci",
    "install:ci:persistence": "cd ./nodes/persistence-node && yarn install --nonInteractive --frozen-lockfile --prefer-offline",
    "install:ci:ens": "cd ./nodes/ens-contenthash-indexer-node && yarn install --nonInteractive --frozen-lockfile --prefer-offline",
  },
  "dependencies": {
  },
  "devDependencies": {
  
  }
}
